export const xTermDarkTheme = {
    background: '#111827',
    foreground: '#00e08f',
    // Cursor for Dark Theme: White block with dark text inside
    cursor: '#F8F8F2',        // Block color (light)
    cursorAccent: '#272822',  // Text color inside block (dark)
};

export const xTermLightTheme = {
    background: '#f9fafb',
    foreground: '#333333',
    // Cursor for Light Theme: Dark block with white text inside
    cursor: '#333333',        // Block color (dark)
    cursorAccent: '#FFFFFF',   // Text color inside block (light)
};
